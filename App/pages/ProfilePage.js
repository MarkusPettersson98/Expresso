import React from "react";
import { AppRegistry, View, Text } from "react-native";

export default (ProfilePage = () => {
    return (
        <View>
            <Text>Profile</Text>
        </View>
    );
});
